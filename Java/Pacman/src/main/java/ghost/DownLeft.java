package ghost;

import processing.core.PImage;
import processing.core.PApplet;

public class DownLeft extends Wall {
    public DownLeft(PImage display, int x, int y) {
        super(display, x, y);
    }
}