package ghost;

import processing.core.PImage;
import processing.core.PApplet;

public class Horizontal extends Wall {
    public Horizontal(PImage display, int x, int y) {
        super(display, x, y);
    }
}