package ghost;

import processing.core.PImage;
import processing.core.PApplet;

public class UpRight extends Wall {
    public UpRight(PImage display, int x, int y) {
        super(display, x, y);
    }
}