package ghost;

import processing.core.PImage;
import processing.core.PApplet;

public class Vertical extends Wall {
    public Vertical(PImage display, int x, int y) {
        super(display, x, y);
    }
}