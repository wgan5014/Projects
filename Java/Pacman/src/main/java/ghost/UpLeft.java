package ghost;

import processing.core.PImage;
import processing.core.PApplet;

public class UpLeft extends Wall {
    public UpLeft(PImage display, int x, int y) {
        super(display, x, y);
    }
}